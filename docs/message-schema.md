# 消息模型与统一通知样式

## 统一卡片样式

所有 interactive card 都遵循同一模板，不使用 emoji，只通过 header 颜色表达严重程度。

| 级别 | 颜色 |
| --- | --- |
| `info` | `blue` |
| `success` | `green` |
| `warning` | `orange` |
| `error` | `red` |
| `critical` | `carmine` |

默认卡片元素顺序：

1. header：标题 + 级别颜色
2. 摘要（可选）
3. 关键字段（服务名、环境、时间，以及自定义双列字段）
4. 自定义元素（可选）
5. 分割线 + 详情（可选）
6. 底部说明 note（可选）

## Text 消息

```json
{
  "msg_type": "text",
  "content": {
    "text": "hello"
  }
}
```

## Post 消息

```json
{
  "msg_type": "post",
  "content": {
    "post": {
      "zh_cn": {
        "title": "deploy finished",
        "content": [
          [{"tag": "text", "text": "app=v1.2.3"}],
          [
            {"tag": "text", "text": "docs: "},
            {"tag": "a", "text": "link", "href": "https://example.com"}
          ]
        ]
      }
    }
  }
}
```

## Interactive Card 消息

```json
{
  "msg_type": "interactive",
  "card": {
    "schema": "2.0",
    "config": {"wide_screen_mode": true},
    "header": {
      "template": "red",
      "title": {"tag": "plain_text", "content": "Order service down"}
    },
    "body": {
      "elements": [
        {
          "tag": "div",
          "text": {"tag": "lark_md", "content": "checkout is failing"}
        },
        {
          "tag": "div",
          "fields": [
            {
              "is_short": true,
              "text": {"tag": "lark_md", "content": "**服务**\norder-api"}
            },
            {
              "is_short": true,
              "text": {"tag": "lark_md", "content": "**环境**\nprod"}
            }
          ]
        },
        {"tag": "hr"},
        {
          "tag": "div",
          "text": {"tag": "lark_md", "content": "error rate > 5% for 10 minutes"}
        },
        {
          "tag": "note",
          "elements": [
            {"tag": "plain_text", "content": "auto-generated by lark_alert"}
          ]
        }
      ]
    }
  }
}
```

## 签名

当配置了 `secret` 时，客户端会在 JSON body 中加入 `timestamp` 和 `sign`：

```json
{
  "timestamp": "1627114387000",
  "sign": "E0zT2kmkJtx1JzyO80J6oXudmfFrYa+qhe/zgjOApR4=",
  "msg_type": "text",
  "content": {"text": "hello"}
}
```

签名算法：

```
sign = base64(hmac_sha256(key = "{timestamp}\n{secret}", message = ""))
```
